# 클라이언트 빠른 설정

## Windows에서 실행하기

### 1. 패키지 설치

```powershell
# 클라이언트 폴더로 이동
cd client

# 패키지 설치
npm install
```

### 2. 클라이언트 실행

```powershell
npm run dev
```

클라이언트가 `http://localhost:5173`에서 실행됩니다!

브라우저에서 `http://localhost:5173`을 열면 사이트를 볼 수 있습니다.


